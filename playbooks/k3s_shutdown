---
- name: Shut down all K3s servers
  hosts: k3s
  gather_facts: false
  become: true  # Ensure the command is run with sudo

  tasks:
    - name: Shut down the server
      command: /sbin/shutdown now
